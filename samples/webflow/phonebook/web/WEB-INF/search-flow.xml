<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE web-flow PUBLIC "-//SPRING//DTD WEB FLOW//EN"
	"http://www.springframework.org/dtd/web-flow.dtd">

<web-flow id="person.Search" start-state="criteria.view">

	<view-state id="criteria.view" view="person.Search.criteria.view">
		<transition on="submit" to="criteria.bindAndValidate"/>
	</view-state>

	<action-state id="criteria.bindAndValidate">
		<action id="person.Search.criteria.bindAndValidate"/>
		<transition on="success" to="executeQuery"/>
		<transition on="error" to="criteria.view"/>
	</action-state>
	
	<action-state id="executeQuery">
		<action id="person.Search.executeQuery"/>
		<transition on="success" to="results.view"/>
	</action-state>
	
	<view-state id="results.view" view="person.Search.results.view">
		<transition on="newSearch" to="criteria.view"/>
		<transition on="select" to="personId.set"/>
	</view-state>

	<action-state id="personId.set">
		<action id="person.Search.id.set"/>
		<transition on="success" to="person.Detail"/>
		<transition on="error" to="error"/>
	</action-state>
	
	<subflow-state id="person.Detail" flow="person.Detail" attribute-mapper="id.attributeMapper">
		<transition on="finish" to="criteria.view"/>
		<transition on="error" to="error"/>
	</subflow-state>
	
	<end-state id="error" view="error.view"/>
	
</web-flow>