<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE beans PUBLIC "-//SPRING//DTD BEAN//EN" "http://www.springframework.org/dtd/spring-beans.dtd">

<beans>

	<!--
		A general purpose FlowController for the entire "Point of Sale (POS)" application.
		Note that there is no default top-level flow configured.
		The flow id of the flow to start should be passed in using the "_flowId" request parameter.
	-->
	<bean name="/pos.htm" class="org.springframework.web.flow.mvc.FlowController">
		<!--
			Set cacheSeconds to a small, non 0, value so you can do ctrl-N to open an new
			window and experiment with the continuations stuff.
		-->
		<property name="cacheSeconds" value="5"/>
		<property name="flowExecutionManager" ref="flowExecutionManager"/>
	</bean>

	<!--
		Custom configuration of the HTTP request flow execution manager.
		Note how you can just change the flow execution storage strategy without any impact
		on the views!
	-->	
	<bean id="flowExecutionManager" class="org.springframework.web.flow.execution.servlet.ServletFlowExecutionManager">
		<property name="storage">
			<bean class="org.springframework.web.flow.execution.servlet.HttpSessionContinuationFlowExecutionStorage"/>
		</property>
		<property name="listener">
			<bean class="org.springframework.samples.sellitem.SellItemFlowExecutionListener"/>
		</property>
		<!-- 
		<property name="storage">
			<bean class="org.springframework.web.flow.execution.servlet.HttpSessionFlowExecutionStorage"/>
		</property>
		-->
		<!-- 
		<property name="storage">
			<bean class="org.springframework.web.flow.execution.ClientContinuationFlowExecutionStorage">
				<property name="compress" value="true"/>
			</bean>
		</property>
		-->
	</bean>
	
	<bean id="sellItem" class="org.springframework.web.flow.config.XmlFlowFactoryBean">
		<property name="location" value="/WEB-INF/sellItem-flow.xml"/>
		<property name="transitionCriteriaCreator">
			<!-- Enable transition criteria expressed as OGNL expressions. -->
			<bean class="org.springframework.web.flow.support.OgnlTransitionCriteriaCreator"/>
		</property>
	</bean>
	
	<bean id="sellItemAction" class="org.springframework.samples.sellitem.SellItemAction">
		<property name="validator">
			<bean class="org.springframework.samples.sellitem.SaleValidator"/>
		</property>
	</bean>

	<bean id="viewResolver" class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<property name="prefix" value="/WEB-INF/jsp/"/>
		<property name="suffix" value=".jsp"/>
	</bean>

</beans>